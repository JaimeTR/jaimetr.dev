interface:
  display_name: "Jaimetr.dev"
  short_description: "Repo-specific patterns and workflows for jaimetr.dev"
  default_prompt: "Use the jaimetr.dev repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true